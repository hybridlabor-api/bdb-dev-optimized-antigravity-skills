# Release Notes

## v1.3.3
- Add OpenWiki native documentation engine.
- Force documentation rebuild if `.openwiki` directory is missing.
- Add environment variables to LaunchAgent plist so `agy` executes authenticated.
- Clean up syntax error in `install_daemon.sh`.

## v1.3.2
- Windows daemon support.
- Scrubbing absolute paths and PII.
- Add openwiki daemon documentation section to README.

## v1.3.1
- Gemini-native `openwiki-skill` and symlink fix.

## v1.2.5
- Restore Homebrew/git manual options in README.
